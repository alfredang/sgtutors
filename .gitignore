node_modules/
dist/
.env
uploads/
*.log
.DS_Store
.playwright-mcp/
*.png
!screenshot.png
gdrive-service-account.json

# Distribution archives (rebuildable — never pushed to GitHub)
*.zip

# Source material the project was derived from — not ours to republish.
**/reference/

# TypeScript incremental build info
*.tsbuildinfo

# Skill symlinks — the real files live in .agents/skills/ (committed).
# `npx skills add` recreates these locally.
.claude/skills/
